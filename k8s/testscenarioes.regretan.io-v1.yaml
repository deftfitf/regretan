# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: testscenarioes.regretan.io
spec:
  group: regretan.io
  names:
    kind: TestScenario
    plural: testscenarioes
    shortNames:
      - ts
    singular: testscenario
  scope: Namespaced
  versions:
    - name: v1
      schema:
        openAPIV3Schema:
          properties:
            spec:
              properties:
                schedule:
                  description: "Specify a schedule to run the scenario. see more detailed\
                  \ information: https://www.manpagez.com/man/5/crontab/"
                  type: string
                steps:
                  description: Specify each step of the test scenario.
                  items:
                    properties:
                      action:
                        description: Actual action to do something for test.
                        properties:
                          kind:
                            description: "Specify the kind of TestAction. If not specified,\
                            \ EmptyAction is used by default."
                            type: string
                          name:
                            description: Specify the name of TestAction. This name will
                              be used in places such as notifications and reports.
                            type: string
                          delay:
                            description: The delay to wait before starting the TestAction.
                              Specifies if a long wait is required after performing
                              the previous Action.
                            type: string
                        x-kubernetes-preserve-unknown-fields: true
                        type: object
                      matchers:
                        description: |-
                          Matchers for cheking whether the step is done correctly.
                          To pass this test step, all the matchers must be true.
                          If no matchers is specified, it will be assumed to be true.
                        items:
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        type: array
                    type: object
                  type: array
              type: object
            status:
              properties:
                synced:
                  type: boolean
                errorMessage:
                  type: string
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: { }
